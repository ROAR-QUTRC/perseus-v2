# Nix folder

This folder is intended to be used mainly as imports for the `flake.nix`. This
reduces the clutter in the `flake.nix`, making it simpler to edit the specific
parts of the nix environment that you want to edit.
